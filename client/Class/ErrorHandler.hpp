#ifndef __ErrorHandler__
#define __ErrorHandler__

class ErrorHandler {
    public:
        ErrorHandler();
        ~ErrorHandler();

        bool isDisplayEnv(char **env);

    private:

    protected:
};

#endif